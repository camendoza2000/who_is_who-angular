+'use strict';

describe('myApp.show module', function() {

  beforeEach(module('myApp.show'));

  describe('show controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var showCtrl = $controller('showCtrl');
      expect(showCtrl).toBeDefined();
    }));

  });
});